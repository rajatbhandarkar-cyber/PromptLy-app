
{
  "type":"module",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.0",
    "express": "^5.1.0",
    "mongoose": "^8.16.4",
    "nodemon": "^3.1.10",
    "openai": "^5.10.1"
  },
  "scripts": {
    "render-build": "npm run build-client && npm run install-server",
    "build-client": "cd ../frontend && npm install && npm run build && rm -rf ../backend/public && mkdir -p ../backend/public && cp -r build/* ../backend/public/",
    "install-server": "npm install",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "engines":{"node":"20.x"}
}

